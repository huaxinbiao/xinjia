<% include ../layout/header.ejs %>
		<div style="margin: 15px;">
			<a href="javascript:;" onclick="location.reload()" class="layui-btn layui-btn-small">刷新</a>
			<fieldset class="layui-elem-field layui-field-title" style="margin-top: 20px;">
				<legend>专业特长设置</legend>
			</fieldset>

			<form class="layui-form" action="">
				<div class="layui-form-item">
					<label class="layui-form-label">第一个名称</label>
					<div class="layui-input-block">
						<input type="text" name="title1" value="<%= toplevel[0][0] %>" lay-verify="title" autocomplete="off" placeholder="请输入" class="layui-input">
						<input type="hidden" name="type" value="professional" />
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第一个描述</label>
					<div class="layui-input-block">
						<input type="text" name="describe1" value="<%= toplevel[0][1] %>" lay-verify="title" placeholder="请输入" autocomplete="off" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">选择分类：</label>
					<div class="layui-input-block">
							<% if(result.length < 1){ %>
								请先添加分类
							<% }else{ %>
								<% result.forEach(function(item, index){ %>
									<input type="checkbox" value="<%= item._id %>" name="ification1" title="<%= item.title %>" 
									<% for(i in toplevel[0][2]){ %>
										<% if(toplevel[0][2][i] == item._id){ %> 
											checked="checked"
										<% } %>
									<% } %>>
								<% }) %>
							<% } %>
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第二个名称</label>
					<div class="layui-input-block">
						<input type="text" name="title2" value="<%= toplevel[1][0] %>" lay-verify="title" autocomplete="off" placeholder="请输入" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第二个描述</label>
					<div class="layui-input-block">
						<input type="text" name="describe2" value="<%= toplevel[1][1] %>" lay-verify="title" placeholder="请输入" autocomplete="off" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">选择分类：</label>
					<div class="layui-input-block">
							<% if(result.length < 1){ %>
								请先添加分类
							<% }else{ %>
								<% result.forEach(function(item, index){ %>
									<input type="checkbox" value="<%= item._id %>" name="ification2" title="<%= item.title %>" 
									<% for(i in  toplevel[1][2]){ %>
										<% if(toplevel[1][2][i] == item._id){ %> 
											checked="checked"
										<% } %>
									<% } %>>
								<% }) %>
							<% } %>
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第三个名称</label>
					<div class="layui-input-block">
						<input type="text" name="title3" value="<%= toplevel[2][0] %>" lay-verify="title" autocomplete="off" placeholder="请输入" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第三个描述</label>
					<div class="layui-input-block">
						<input type="text" name="describe3" value="<%= toplevel[2][1] %>" lay-verify="title" placeholder="请输入" autocomplete="off" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">选择分类：</label>
					<div class="layui-input-block">
							<% if(result.length < 1){ %>
								请先添加分类
							<% }else{ %>
								<% result.forEach(function(item, index){ %>
									<input type="checkbox" value="<%= item._id %>" name="ification3" title="<%= item.title %>" 
									<% for(i in toplevel[2][2]){ %>
										<% if(toplevel[2][2][i] == item._id){ %> 
											checked="checked"
										<% } %>
									<% } %>>
								<% }) %>
							<% } %>
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第四个名称</label>
					<div class="layui-input-block">
						<input type="text" name="title4" value="<%= toplevel[3][0] %>" lay-verify="title" autocomplete="off" placeholder="请输入" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">第四个描述</label>
					<div class="layui-input-block">
						<input type="text" name="describe4" value="<%= toplevel[3][1] %>" lay-verify="title" placeholder="请输入" autocomplete="off" class="layui-input">
					</div>
				</div>
				<div class="layui-form-item">
					<label class="layui-form-label">选择分类：</label>
					<div class="layui-input-block">
							<% if(result.length < 1){ %>
								请先添加分类
							<% }else{ %>
								<% result.forEach(function(item, index){ %>
									<input type="checkbox" value="<%= item._id %>" name="ification4" title="<%= item.title %>" 
									<% for(i in toplevel[3][2]){ %>
										<% if(toplevel[3][2][i] == item._id){ %> 
											checked="checked"
										<% } %>
									<% } %>>
								<% }) %>
							<% } %>
					</div>
				</div>
				<div class="layui-form-item">
					<div class="layui-input-block">
						<button class="layui-btn" lay-submit="" lay-filter="demo1">保存</button>
					</div>
				</div>
			</form>
		</div>
		<script type="text/javascript" src="/BeginnerAdmin/plugins/layui/layui.js"></script>
		<script>
			layui.use(['form', 'layedit', 'laydate', 'jquery'], function() {
				var form = layui.form(),
					layer = layui.layer,
					layedit = layui.layedit,
					laydate = layui.laydate,
					$ = layui.jquery;
				var t_fun = false;
				//自定义验证规则
				form.verify({
					title: function(value) {
						if(value.length > 100 || value.length <= 0) {
							return '输入字数不正确';
						}
					}
				});
				//监听提交
				form.on('submit(demo1)', function(data) {
					if(t_fun){
						return false;
					}
					t_fun = true;
					data.field = $('form').serialize();
					$.post('/admin/toplevel/professional', data.field, function(res){
						if(res.code == 200){
							layer.msg('保存成功', function(){
								location.reload();
							});
						}else{
							layer.msg(res.msg);
							t_fun = false;
						}
					})
					return false;
				});
			});
		</script>
<% include ../layout/footer.ejs %>